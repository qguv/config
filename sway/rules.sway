for_window {

    [app_id="nm-connection-editor"] {
        floating enable;
        sticky enable;
    }

    # xev
    [title="Event Tester"] {
        floating enable;
        sticky enable;
    }

    # visual studio code
    [class="Code"] {
        border pixel;
        splitv;
    }

    [app_id="gnome-terminal-server"] opacity 0.95
    [app_id="alacritty"] opacity 0.95
}
